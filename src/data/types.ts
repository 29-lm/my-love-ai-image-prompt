export interface PromptItem {
  id: number;
  title: string;
  slug: string;
  en_prompt: string;
  zh_prompt: string;
  images: string[];
}

export interface PromptIndexItem {
  id: number;
  title: string;
  slug: string;
  images: string[];
}
